/**
 * Just to test JSDoc Generation to README.md
 * @returns 0
 */
function createTest() {
    return 0
}

/**
 * Adding two numbers
 * @param {Number} num1 - assumed
 * @param {Number} num2 - assumed
 * @returns sum of num1 + num2
 */
function hello(num1, num2){
    let sum = num1 + num2;
    return sum;
}

/**
 * Begging, please render documentation
 * @param {String} answer 
 * @returns {String} "thanks"
 */
function pleaseRender(answer) {
    return "thanks";
}
